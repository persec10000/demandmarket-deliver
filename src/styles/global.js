import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    dropShadow: {
        shadowColor: "#000",
        shadowOffset: {
            width: 8,
            height: 4,
        },
        shadowOpacity: 0.3,
        shadowRadius: 28,

        elevation: 5,
    }
});
